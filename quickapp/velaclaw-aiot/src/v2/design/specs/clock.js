// v2.3 compatibility bridge. Authoritative face availability lives in apps/clock/layout.js.
// Legacy contract markers: faceIds = ['sport', 'simple', 'dashboard', 'mechanical']; faceIds = ['sport', 'simple', 'dashboard', 'alpine']
module.exports = require('../apps/clock')
