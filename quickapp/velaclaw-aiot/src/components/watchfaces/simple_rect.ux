<template>
  <div class="rect-simple-face">
    <div class="simple-top"><text class="simple-week" style="color: {{ faceAccent }};">{{ displayWeek }}</text><text class="simple-date">{{ displayMonth }} {{ displayDate }}</text></div>
    <text class="simple-time">{{ displayHours }}:{{ displayMinutes }}</text>
    <div class="simple-goal-row"><text class="simple-goal-label">今日目标</text><text class="simple-goal-value" style="color: {{ faceAccent }};">{{ goalPercent }}%</text></div>
    <div class="simple-goal-track"><div class="simple-goal-fill" style="width: {{ goalPercent }}%; background-color: {{ faceAccent }};"></div></div>
    <div class="simple-metrics">
      <div class="simple-metric" @click="openSteps"><text class="simple-label">步数</text><text class="simple-value simple-steps">{{ stepsText }}</text></div>
      <div class="simple-metric simple-middle" @click="openHealth"><text class="simple-label">心率</text><text class="simple-value simple-heart">{{ currentHeartRate }}</text></div>
      <div class="simple-metric"><text class="simple-label">电量</text><text class="simple-value" style="color: {{ batteryColor }};">{{ batteryPercent }}%</text></div>
    </div>
  </div>
</template>

<script>
export default {
  props: ['displayWeek','displayMonth','displayDate','displayHours','displayMinutes','faceAccent','goalPercent','stepsText','currentHeartRate','batteryPercent','batteryColor'],
  openHealth() { this.$emit('openHealth') },
  openSteps() { this.$emit('openSteps') }
}
</script>

<style>
.rect-simple-face { width: 192px; height: 100%; flex-direction: column; align-items: center; background-color: #05070B; }
.simple-top { width: 164px; height: 24px; margin-top: 16px; flex-direction: row; justify-content: space-between; align-items: center; }
.simple-week { font-size: 9px; font-weight: bold; } .simple-date { font-size: 8px; color: #8E8E93; }
.simple-time { width: 164px; height: 58px; margin-top: 4px; font-size: 44px; color: #FFFFFF; font-weight: bold; text-align: center; }
.simple-goal-row { width: 164px; height: 18px; margin-top: 7px; flex-direction: row; justify-content: space-between; align-items: center; }
.simple-goal-label { font-size: 7px; color: #8E8E93; } .simple-goal-value { font-size: 9px; font-weight: bold; }
.simple-goal-track { width: 164px; height: 4px; border-radius: 2px; background-color: #20242B; }
.simple-goal-fill { height: 4px; border-radius: 2px; }
.simple-metrics { width: 164px; height: 54px; margin-top: 15px; flex-direction: row; justify-content: center; }
.simple-metric { width: 50px; height: 54px; border-radius: 10px; background-color: #12161D; flex-direction: column; justify-content: center; align-items: center; }
.simple-middle { margin-left: 7px; margin-right: 7px; }
.simple-label { width: 46px; font-size: 7px; color: #8E8E93; text-align: center; }
.simple-value { width: 46px; margin-top: 5px; font-size: 14px; font-weight: bold; text-align: center; lines: 1; }
.simple-steps { font-size: 12px; color: #FFD60A; } .simple-heart { color: #FF375F; }
</style>
