name: Temporary V2 Runtime Allocation Check

on:
  push:
    branches:
      - perf/v2-runtime-allocation-20260912

jobs:
  check:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: quickapp/velaclaw-aiot
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: npm
          cache-dependency-path: quickapp/velaclaw-aiot/package-lock.json
      - name: Install dependencies
        run: npm ci
      - name: Run V2 contracts
        run: npm run check
      - name: Build QuickApp
        run: npm run build
